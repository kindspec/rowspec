# Why this case exists

§7: `delta(c)` is `c - prior(c)`, and on the blank row `c` itself is blank —
"a difference needs a number." The wrong answers are `-5.0` (blank read as
zero) and blank (the operator quietly declining). Both are quiet; the spec
mandates `#REF!(amt)`.

# Why this case exists

§4.2 rule 2: "`/` is real division: `7 / 2` is `3.5`, never `3`." The
alternative "makes `qty / 2` depend on whether a cell is spelled `7` or `7.0`,
and §4.1.6 refused a second spelling of a number precisely so that no value in
the format can depend on that."
